interface:
  display_name: "PDF Word Skill"
  short_description: "Rebuild editable Word pages from PDFs."
  default_prompt: "Use $pdf-word-skill to rebuild a PDF into editable Word, compare each page, and adjust text and image positions."
